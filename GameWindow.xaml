<Window x:Class="TaikoProject.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaikoProject"
        mc:Ignorable="d"
        Title="Taiko Game" Height="650" Width="1000"
        Background="#FF1E1E1E"
        WindowStartupLocation="CenterScreen"
        Focusable="True" KeyDown="GameWindow_KeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- top: score and combo -->
        <Border Grid.Row="0" Background="#FF2D2D30" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" x:Name="SongTitleText" Text="Now Playing: Example Song" 
                           VerticalAlignment="Center" FontSize="18" Foreground="#FFD15858" FontWeight="Bold"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="COMBO: " VerticalAlignment="Center" Foreground="White" FontSize="16"/>
                    <TextBlock x:Name="ComboText" Text="0" VerticalAlignment="Center" Foreground="#FF5BC0DE" FontSize="18" FontWeight="Bold" Margin="5,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="SCORE: " VerticalAlignment="Center" Foreground="White" FontSize="16"/>
                    <TextBlock x:Name="ScoreText" Text="0" VerticalAlignment="Center" Foreground="#FFD15858" FontSize="18" FontWeight="Bold" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- game area: note track -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 上轨道装饰 -->
            <Border Grid.Row="0" Background="#FF3E3E40" CornerRadius="5" Margin="0,0,0,10"/>
            
            <!-- 主游戏区域 -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                
                <!-- 音符滚动区域 -->
                <Canvas x:Name="NoteCanvas" Background="Transparent">
                    <!-- 音符将在代码中动态添加到这里 -->
                </Canvas>
                
                <!-- 击打区域（太鼓） -->
                <Border Grid.Column="1" Background="#FFD15858" CornerRadius="10" Margin="20,0,0,0" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#FFB84747" CornerRadius="5" Margin="5">
                            <TextBlock Text="RED" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </Border>
                        
                        <Border Grid.Row="1" Background="#FF47B8B8" CornerRadius="5" Margin="5,0,5,5">
                            <TextBlock Text="BLUE" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                       FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- 下轨道装饰 -->
            <Border Grid.Row="2" Background="#FF3E3E40" CornerRadius="5" Margin="0,10,0,0"/>
        </Grid>

        <!-- bottom: judgement -->
        <Border Grid.Row="2" Background="#FF2D2D30" Padding="10">
            <TextBlock x:Name="JudgmentText" Text="" FontSize="28" FontWeight="Bold" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>